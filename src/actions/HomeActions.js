import { COMMON_SET_PEOPLE } from "../reducers/types"

export const setPeople = (people) => {

    return {
        type: COMMON_SET_PEOPLE,
        payload: people
    }
}
export const getPeople = (people) => {

    
}